// The check engine (PROJECT_CONTEXT §6 M5): pure functions over a captured network trace,
// so it is testable without a browser. Types for the trace arrive with the runner in M4;
// the ten checks arrive in M5.
export {};
